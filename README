This repo might be a bitrotted mirror of some of the implementation. Canonical implementation is at:

https://github.com/brownplt/lambda-py

This is (was) just the necessary source + dependencies to build the paper

